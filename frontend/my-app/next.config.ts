// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com'],
    // También puedes agregar otros dominios que uses
    // domains: ['images.unsplash.com', 'tusitio.com', ...],
  },
}

module.exports = nextConfig

// next.config.js
module.exports = {
  images: {
    domains: ['images.unsplash.com'],
  },
}